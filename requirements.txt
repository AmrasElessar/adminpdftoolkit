# Production dependencies — version-pinned for reproducible installs.
# Keep in sync with pyproject.toml [project.dependencies].
pymupdf>=1.27,<2.0
pdfplumber>=0.11,<1.0
pdf2docx>=0.5.12,<1.0
openpyxl>=3.1,<4.0
python-docx>=1.1,<2.0
xhtml2pdf>=0.2.16,<1.0
# pycairo is pulled in transitively by xhtml2pdf → svglib → rlpycairo.
# 1.29.0 dropped Linux wheels (sdist-only) and needs libcairo2-dev to
# build, which CI runners and the slim Docker base don't have.
# Cap below 1.29 so pip stays in wheel territory until upstream re-ships.
pycairo>=1.20,<1.30
Pillow>=10.0,<13.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.20,<1.0
jinja2>=3.1,<4.0
easyocr>=1.7,<2.0
cryptography>=43.0,<48.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
